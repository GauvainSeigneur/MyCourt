<?xml version="1.0" encoding="utf-8"?>
<transitionSet
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <transitionSet>
        <targets>
            <target android:excludeId="@android:id/navigationBarBackground" />
            <target android:excludeId="@android:id/statusBarBackground" />
            <target android:excludeId="@id/app_bar" />
            <target android:excludeId="@id/toolbar" />
            <target android:excludeId="@id/shot_image" />
            <target android:excludeId="@id/status_bar_background_layout" />
            <target android:excludeId="@id/constant_status_bar_background" />
            <target android:excludeId="@id/custom_status_bar_background" />
        </targets>
        <slide android:slideEdge="bottom"
             android:duration="350"
             android:interpolator="@android:interpolator/linear_out_slow_in" >
             <targets>
                 <target android:excludeId="@+id/app_bar"/>
                 <target android:excludeId="@id/toolbar" />
            </targets>
        </slide>
     <!-- Delay fading in so that the shared element transition on the background
          has time to run -->
        <fade
            android:startDelay="250"
            android:duration="100"
            android:interpolator="@android:interpolator/linear" />
    </transitionSet>
</transitionSet>